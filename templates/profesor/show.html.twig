{% extends 'base.html.twig' %}

{% block title %}Profesor{% endblock %}

{% block body %}

    <h2 class="text-center font-weight-bold">Profesor</h2>
    <div class="table-responsive">
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profesor.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ profesor.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ profesor.apellido }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ profesor.telefono }}</td>
            </tr>
        </tbody>
    </table>
</div>
    <a class="btn btn-primary" href="{{ path('app_profesor_index') }}">Volver a la lista</a>

    <a class="btn btn-success" href="{{ path('app_profesor_edit', {'id': profesor.id}) }}">Editar</a>

    {{ include('profesor/_delete_form.html.twig') }}
{% endblock %}
